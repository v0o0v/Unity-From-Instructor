using UnityEngine;

public class StudyVariable : MonoBehaviour
{
    int number1 = 10;
    int number2 = -500;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Hello");
    }
    
    // Update is called once per frame
    void Update()
    {
        Debug.Log("Hello");
    }
}